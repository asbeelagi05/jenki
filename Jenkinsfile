pipeline {
    agent any
    stages {
        stage('Clone') {
            steps {
                git url: 'pipeline {
    agent any
    stages {
        stage('Clone') {
            steps {
                git url: 'https://github.com/yourusername/reponame.git',
                    branch: 'main'
            }
        }
        stage('Run Script') {
            steps {
                sh 'chmod +x script.sh'
                sh './script.sh'
            }
        }
    }
}
',
                    branch: 'main'
            }
        }
        stage('Run Script') {
            steps {
                sh 'chmod +x script.sh'
                sh './script.sh'
            }
        }
    }
}
